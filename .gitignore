# Local editor / OS
.DS_Store
*.swp
*~
.idea/
.vscode/

# Nix (when the flake arrives)
result
result-*
.direnv/
.pre-commit-config.yaml.local

# Private / vault-only (never commit)
docs/FLEET.md
docs/WORKSPACE.md
AGENTS.hedronite.md
.hedronite/
PRIVATE.md
